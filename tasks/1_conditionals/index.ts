// Execute the leaky reLU function
// Returns x if x > 0
// Returns x times a otherwise
export function leakyRelu(x: number, a = 0.1): number {
  // TODO: implement using conditional branching
  return 0;
}

// Execute the reLU function
// Returns x if x > 0
// Returns 0 otherwise
export function relu(x: number): number {
  // TODO: implement, without using conditional branching (no if statements, no ternary operator), but only using a function call
  return 0;
}

// More info
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/if...else

// Visualisation
// https://www.codeproject.com/KB/AI/1220276/ReLU.png
